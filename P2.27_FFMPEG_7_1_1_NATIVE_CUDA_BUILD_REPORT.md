# P2.27 — Native FFmpeg 7.1.1 CUDA Build Report

**Date:** 2026-08-15  
**Status:** **GO FOR P2.28**  
**Scope:** Isolated native FFmpeg 7.1.1 MSVC build with CUDA/NVDEC/NVENC, CUDA AVFrames, CUDA filters, and libavfilter development API.

## Result

The isolated build, installation, capability checks, native API probe, zero-copy CUDA pipeline smoke test, ABI checks, and requested regression checks all passed. The existing `C:\ffmpeg\bin\ffmpeg.exe`, project `.venv`, CuPy installation, production source, P2.20/P2.23 artifacts, and existing outputs were not used as build/install locations and were not replaced.

No custom FFmpeg filter, CUDA transform, HDR kernel, renderer, benchmark, full-video render, or production transform was implemented.

## Isolation and exact inputs

All P2.27 build sources, headers, libraries, intermediate objects, installed files, validation output, and logs are under:

`G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg-build\`

Directory layout used:

- `source\ffmpeg-7.1.1\`
- `downloads\`
- `nv-codec-headers\nv-codec-headers-n12.2.72.0\`
- `build\`
- `install\`
- `logs\`
- `pkgconfig\`
- `api-probe\`
- `validation\`

Exact official FFmpeg source:

- Release: `FFmpeg 7.1.1`
- Archive: `downloads\ffmpeg-7.1.1.tar.xz`
- SHA-256: `733984395E0DBBE5C046ABDA2DC49A5544E7E0E1E2366BBA849222AE9E3A03B1`
- Source marker: `source\ffmpeg-7.1.1\RELEASE = 7.1.1`

CUDA codec headers:

- Tag: `n12.2.72.0`
- SHA-256: `DBEAEC433D93B850714760282F1D0992B1254FC3B5A6CB7D76FC1340A1E47563`
- Local pkg-config version: `12.2.72.0`
- FFmpeg 7.1.1 requirement: `ffnvcodec >= 12.1.14.0`

No `master`, FFmpeg 8.x, FFmpeg 9.x, arbitrary snapshot, NASM, YASM, MinGW toolchain switch, global codec-header installation, or system FFmpeg replacement was used.

## Toolchain

- OS: Windows x64
- GPU: NVIDIA GeForce RTX 3080, compute capability 8.6
- NVIDIA driver: `595.79`
- CUDA Toolkit: `12.9.86`, `nvcc.exe` under `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9\bin`
- MSVC: Visual Studio 2022, compiler `19.44.35217`, x64 host/target
- GNU Make: MinGW `make.exe` used only as the build helper from Git Bash
- x86 assembly: disabled because NASM/YASM were absent

## Configure

Configure was run before any full build and passed. Log:

`dev\ffmpeg-build\logs\config.log`

The configured scope included:

- `--toolchain=msvc`
- `--arch=x86_64 --target-os=win32`
- `--enable-shared --disable-static`
- `--enable-nonfree`
- `--enable-cuda-nvcc --nvccflags='-gencode arch=compute_86,code=sm_86'`
- `--enable-ffnvcodec --enable-cuvid --enable-nvdec --enable-nvenc`
- CUDA hardware accelerators and filters
- `--disable-x86asm`
- `--disable-libnpp --disable-opencl --disable-vulkan`
- `--disable-d3d11va --disable-d3d12va --disable-dxva2`
- `--disable-amf --disable-libvpl`
- `--disable-doc --disable-ffplay`

Generated configure outputs include `config.h`, `config_components.h`, and `ffbuild\config.mak`. Configure confirms CUDA NVCC, FFNVCODEC, CUVID, NVDEC, NVENC, AVFILTER, and x86-64 support.

## Build and install

A single-object MSVC build check passed before the full build:

`make -j1 CCDEP= CXXDEP= ASDEP= HOSTCCDEP= V=1 libavcodec/amrwbdec.o`

The first full parallel attempt exposed Windows command-length failures in the unmodified Make-to-shell/link path. No FFmpeg source was changed. Two isolated wrapper scripts under `dev\ffmpeg-build\logs\` resolved the transport limits:

- `makedef-msvc.sh` passes only short version-script/target arguments through Make, then discovers compiled objects and invokes the unchanged FFmpeg `compat/windows/makedef`.
- `mslink-response.sh` writes linker arguments to a temporary response file, reconstructs complete library object trees when the native Make command truncates an object token, and restores configured import-library/system-library dependencies.

The final clean build rerun returned:

```text
make: Nothing to be done for 'all'.
BUILD_EXIT=0
```

Build log:

`dev\ffmpeg-build\logs\build.log`

Installation used only the configured prefix and returned:

```text
INSTALL_EXIT=0
```

Install log:

`dev\ffmpeg-build\logs\install.log`

Installed binaries:

- `dev\ffmpeg-build\install\bin\ffmpeg.exe`
- `dev\ffmpeg-build\install\bin\ffprobe.exe`
- `dev\ffmpeg-build\install\bin\avutil-59.dll`
- `dev\ffmpeg-build\install\bin\avcodec-61.dll`
- `dev\ffmpeg-build\install\bin\avformat-61.dll`
- `dev\ffmpeg-build\install\bin\avfilter-10.dll`
- `dev\ffmpeg-build\install\bin\avdevice-61.dll`
- `dev\ffmpeg-build\install\bin\swscale-8.dll`
- `dev\ffmpeg-build\install\bin\swresample-5.dll`

MSVC import libraries are installed under `dev\ffmpeg-build\install\bin\` and export definition files under `dev\ffmpeg-build\install\lib\`.

## Isolated runtime capabilities

The absolute isolated executable was invoked with the isolated DLL directory first in `PATH`. `ffmpeg -version`, `-buildconf`, `-hwaccels`, `-encoders`, `-decoders`, and `-filters` passed the following checks:

- FFmpeg version: `7.1.1`
- Hardware acceleration: `cuda`
- CUVID/NVDEC decoders present, including `h264_cuvid` and `hevc_cuvid`
- `h264_nvenc` present
- `hevc_nvenc` present
- `scale_cuda` present
- `colorspace_cuda` present

The installed configuration printed by the isolated executable matches the requested MSVC/CUDA/NVDEC/NVENC/shared-only configuration.

## Zero-copy CUDA frame-path validation

Input was an existing short HEVC Main 10 sample, used only as input:

`test_output\BR2049_nvenc_10f.mkv`

The two-frame validation output and verbose log are isolated under:

- `dev\ffmpeg-build\validation\zero_copy_hevc_2frames.mkv`
- `dev\ffmpeg-build\validation\zero_copy_verbose.log`

Pipeline command:

```text
ffmpeg.exe -y -hide_banner -loglevel verbose -hwaccel cuda -hwaccel_output_format cuda -c:v hevc_cuvid -i BR2049_nvenc_10f.mkv -vf scale_cuda=1920:1080 -c:v hevc_nvenc -frames:v 2 -f matroska zero_copy_hevc_2frames.mkv
```

Result:

- `ZERO_COPY_EXIT=0`
- `HEVC_CUVID=True`
- `SCALE_CUDA=True`
- `HEVC_NVENC=True`
- `OUTPUT_PRESENT=True`
- `FORBIDDEN_GRAPH_TERMS_ABSENT=True`

The verbose graph confirmed:

```text
hevc (hevc_cuvid) -> hevc (hevc_nvenc)
Formats: Original: cuda | HW: cuda | SW: p010le
Parsed_scale_cuda_0 ... fmt:p010le -> ... fmt:p010le
Using input frames context (format cuda) with hevc_nvenc encoder.
Nvenc initialized successfully
2 frames encoded
```

The graph contained no `hwdownload`, no `hwupload_cuda`, and no rawvideo pipe.

## Native development API probe

Probe source and executable:

- `dev\ffmpeg-build\api-probe\api_probe.c`
- `dev\ffmpeg-build\api-probe\api_probe.exe`

The probe included and exercised:

- `libavutil/frame.h` and `AVFrame`
- `libavutil/hwcontext.h`, `AVHWDeviceContext`, and `AVHWFramesContext`
- `libavutil/hwcontext_cuda.h` and `AVCUDADeviceContext`
- `AVFrame.hw_frames_ctx`
- CUDA hardware pixel format and P010 software format fields
- Isolated `avutil.lib` and `avutil-59.dll`

Results:

```text
API_COMPILE_EXIT=0
api_probe_ok avutil=3876708 ... hwformat=117 swformat=158
API_RUN_EXIT=0
```

No global headers or libraries were used.

## ABI and installation checks

The isolated executable reported the required major versions:

- `libavutil 59`
- `libavcodec 61`
- `libavformat 61`
- `libavfilter 10`

All required headers, DLLs, import libraries, and `.def` files were present. The required-file assertion reported:

```text
REQUIRED_FILE_COUNT=18
MISSING_COUNT=0
```

## Allowed regression checks

- `nvidia-smi`: RTX 3080, compute capability 8.6, driver 595.79, 10240 MiB
- Existing CuPy: `14.1.1`, CUDA runtime `13020`, check exit `0`
- Existing `C:\ffmpeg\bin\ffmpeg.exe`: still present, size `87429632` bytes, version `7.1.1-essentials_build-www.gyan.dev`; it was not replaced or used for the isolated validation
- Existing project test suite: `342 passed, 4 warnings` in `48.48s`, `PYTEST_EXIT=0`

The warnings were the pre-existing HLG runtime warnings and a pytest cache permission warning; no test failed.

## Isolation and repository safety

The final audit confirmed the protected existing FFmpeg and `.venv` files remained present. The accidental workspace-root `api_probe.obj` emitted by the first MSVC probe compile was removed; the retained probe source/executable are under `dev\ffmpeg-build\api-probe\`.

The worktree already contained unrelated P2.17–P2.26 untracked artifacts and pre-existing source modifications. They were not edited by this task. No commit or push was performed.

## Decision

**STATUS = GO FOR P2.28**

The native FFmpeg 7.1.1 CUDA/NVDEC/NVENC/libavfilter development foundation is validated. Stop here for P2.27; do not implement the custom transform or production renderer as part of this task.
