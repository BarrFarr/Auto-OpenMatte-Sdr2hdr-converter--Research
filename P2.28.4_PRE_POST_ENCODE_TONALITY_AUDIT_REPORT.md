# P2.28.4 — Pre-Encode vs Post-Encode Tonality Audit

**Status:** `PASS`  
**Frame pair:** HDR `73367` / Open Matte `73348`  
**Mode:** read-only; no algorithm, production code, CUDA backend, fitting, LUT, sampling, geometry, synchronization, render, or production encode was changed.

## Executive conclusion

1. **Czy różnica istnieje już BEFORE NVENC?**  
   W zachowanym composited CUDA pre-encode output różnica względem HDR mastera wynosi `0` w chronionym HDR core. Pełny overlap zawiera wyłącznie oczekiwaną czteropikselową strefę feather i jest raportowany osobno. Nie należy interpretować tej strefy jako globalnego błędu tonu.
2. **Ile dodatkowego błędu wprowadza NVENC/decode?**  
   Dla pełnej klatki B→C: RGB mean absolute `0.0042002205`, RGB RMSE `0.00543930336`, RGB P99 `0.0154733528`, RGB max `0.0532163494`. Luminance mean absolute `0.585301608` nits, RMSE `1.267072` nits, P99 `5.48780983` nits, max `24.756679` nits.
3. **Czy problem jest głównie GLOBAL_TONAL_FIT?**  
   `GLOBAL_TONAL_FIT = SUPPORTED` dla zachowanego transform-only reference z P2.28.3: mean absolute residual `9.33650917` nits, RMSE `18.752527` nits, P99 `74.3371124` nits, max `210.304437` nits. To jest osobny pomiar niecomposited Open Matte→HDR transformu, bez nowego fittingu.
4. **Czy ENCODE/PACKING dokłada istotny błąd?**  
   `ENCODE/PACKING = SUPPORTED` na poziomie pomiaru. B→C jest mierzalne, ale raport nie narzuca nieistniejącego progu percepcyjnego.
5. **Czy oba problemy występują jednocześnie?**  
   `SUPPORTED`: transform-only residual i encode/decode residual są mierzalne jako dwa oddzielne etapy. Protected composited HDR core pozostaje niezmieniony.

## Exact inputs and correspondence

- HDR master: `G:\Filmy\The Matrix UHD\The Matrix.mkv`
- Open Matte: `G:\Filmy\IMAX format (open matte)\The Matrix (1999) [OPEN MATTE] [WEB-DL 1080p 10bit DD5.1 x265].mkv`
- HDR frame: `73367`
- Open Matte frame: `73348`
- Frame offset: `-19`; convention `OM_frame = HDR_frame - 19`
- Accepted confidence: `0.940513`; status `LOCKED`
- No synchronization was rerun. The source movie files were not scanned or decoded in full.
- A/B source: preserved P2.28.2 float64 NPY snapshots.
- C source: exactly one frame decoded from the existing `G:\Auto-OpenMatte-Sdr2hdr-converter\test_output\P2.28.2_TheMatrix_CUDA_51m_2s.mkv`.

## Geometry and comparison scope

- HDR master: `3840x1600`; Open Matte/output: `1920x1080`.
- Active HDR overlap in output coordinates: `x=0:1920`, `y=140:940`.
- Generated regions: top `y=0:140`; bottom `y=940:1080`.
- Feather width: `4 px`.
- Protected core: output `y=144:936`, `x=4:1916`.
- All primary A/B comparisons use the corresponding active overlap. B/C is reported for both active overlap and the full 1080p frame.
- The original HDR master does not contain corresponding pixels for the generated top/bottom regions; profile values are therefore `n/a` there rather than invented.

## Three-way comparison

| metric | HDR master vs CUDA pre-encode | CUDA pre vs CUDA decoded |
|---|---:|---:|
| RGB max | `0.387305552` | `0.0532163494` |
| RGB RMSE | `0.0081196977` | `0.00528128367` |
| RGB P99 | `0.012909852` | `0.0146946443` |
| luminance max (nits) | `105.013485` | `24.756679` |
| luminance RMSE (nits) | `1.43487642` | `1.38069564` |
| luminance P99 (nits) | `0.411351181` | `6.03605508` |

A/B protected HDR core: RGB max `0`, RGB mean `0`, invariant pass `True`. Full-overlap A/B values above include the expected feather transition.

## Spatial analysis

The overlap is divided into A (`>100 px` from either seam), B (`50–100 px`), and C (`0–50 px`), with top/bottom seam bands also separated. Each band is cross-tabulated by HDR-master luminance: shadow `<10 nits`, midtone `10–100 nits`, highlight `>=100 nits`. Complete RGB, luminance, Y, Cb, and Cr metrics are in:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg-build\validation\P2284_TheMatrix_pre_post_encode_metrics.json
```

The primary spatial entries are under `spatial_analysis.pairs`. No spatial smoothing was applied.

## Tonal profiles

The JSON contains unsmoothed 200-row profiles crossing both seams at X positions 25%, 50%, and 75%. Each row contains `HDR master`, `CUDA pre-encode`, and `CUDA decoded` PQ luminance. HDR values are present only on the active-master side of each seam; generated-side values are explicitly `n/a`.

## RGB and colour separation

For both A/B and B/C the JSON contains:

- R, G, B signal residuals with mean, median, RMSE, P95, P99, and max;
- Y signal and PQ luminance residuals with the same statistics;
- BT.2020-derived Cb and Cr residuals;
- shadow, midtone, highlight, A/B/C, top-seam, and bottom-seam subsets.

This separates the pre-existing transform/reference residual from the encode/decode residual without changing either path.

## Transform-only reference (reused, no new fitting)

The transform-only context is copied from the preserved P2.28.3 metrics artifact. It compares the unchanged existing-curve transformed Open Matte overlap against the resized HDR master overlap through the unchanged `CPUTransformBackend`; it is not a new fit and it is not substituted for the composited A/B invariant. Its overall residual is:

- mean absolute: `9.33650917` nits;
- RMSE: `18.752527` nits;
- absolute P95/P99/max: `45.1836349` / `74.3371124` / `210.304437` nits.

## Encode/decode-only result

For the exact same preserved B frame and one decoded C frame, the full-frame B→C residual is:

- RGB mean/median/RMSE/P95/P99/max: `0.0042002205` / `0.00340127035` / `0.00543930336` / `0.010749567` / `0.0154733528` / `0.0532163494`;
- luminance mean/median/RMSE/P95/P99/max: `0.585301608` / `0.100107916` / `1.267072` / `2.71197842` / `5.48780983` / `24.756679` nits.

## Validation

- All HDR, Open Matte, pre-encode, decoded, and derived active-overlap arrays are finite: `True`.
- Resolutions and shapes are correct: `True`.
- Exact frame correspondence is inherited and unchanged: `True`.
- Existing CUDA MKV HDR metadata checks: `True`.
- Existing CUDA MKV 48-frame `-xerror` decode/corruption probe: `True`.
- Protected HDR overlap invariant: `True`.
- White dots above 400 nits in generated extension: pre `0`, decoded `0`; check pass `True`.

## Diagnostic snapshots

Directory:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg-build\validation\P2284_TheMatrix_pre_post_encode
```

It contains PNGs for the HDR master, CUDA pre-encode, CUDA decoded frame, the active HDR overlap, and top/bottom seam crops for all three states. Exact float64 NPY snapshots are also written for A/B/C; the original P2.28.2 NPY and RGB48LE artifacts remain the source-of-truth for preserved data. PNGs are neutral uint16 display diagnostics only.

## Scope confirmation

All P2.28.4 scope flags are in `scope_constraints` in the JSON and are `false` for prohibited actions. No production code, CUDA backend, transform mathematics, fitted curve, LUT, sampling, geometry, compose, feather/blending, synchronization, production NVENC settings, or source-film data was modified. No commit or push was performed.
