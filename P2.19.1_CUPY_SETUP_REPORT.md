# P2.19.1 — CuPy GPU Setup Report

**Date:** 2026-08-15  
**Status:** PASS — CuPy installed in the project virtual environment and GPU smoke tests passed.

## Scope

This task only prepared and verified the GPU environment. No GPU transform benchmark, GPU transform implementation, render, production-pipeline change, commit, or push was performed.

## Python and virtual environment

- Project virtual environment: `G:\Auto-OpenMatte-Sdr2hdr-converter\.venv`
- Python executable: `G:\Auto-OpenMatte-Sdr2hdr-converter\.venv\Scripts\python.exe`
- pip executable: `G:\Auto-OpenMatte-Sdr2hdr-converter\.venv\Scripts\pip.exe`
- Python: `3.13.7`
- pip: `26.2.1`

## CUDA/CuPy selection and installation

The NVIDIA driver reports CUDA 13.2. PyPI exposed a compatible `cp313-cp313-win_amd64` wheel for the CUDA 13 CuPy variant, so the pinned installation was:

```text
cupy-cuda13x[ctk]==14.1.1
```

It was installed only through the project virtual environment's pip executable:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\.venv\Scripts\pip.exe install --only-binary=:all: "cupy-cuda13x[ctk]==14.1.1"
```

The `[ctk]` extra installed the minimal PyPI CUDA runtime/component packages required by CuPy inside `.venv`. It did **not** install a system CUDA Toolkit or `nvcc`; final check: `nvcc: NOT FOUND`.

## NVIDIA device

- GPU: `NVIDIA GeForce RTX 3080`
- VRAM reported by `nvidia-smi`: `10240 MiB`
- NVIDIA driver: `595.79`
- CUDA version reported by `nvidia-smi`: `13.2`
- CuPy device count: `1`
- Compute capability: `86`
- CuPy runtime reported by `cp.cuda.runtime.runtimeGetVersion()`: `13020` (CUDA 13.2)

## CuPy functional test

The test used CuPy arrays, GPU multiplication, explicit null-stream synchronization, and host transfer:

```text
cupy_version: 14.1.1
cuda_runtime: 13020
device_count: 1
device_name: NVIDIA GeForce RTX 3080
compute_capability: 86
functional_test: [0.0, 1.0, 4.0, 9.0]
```

**Result: PASS.** The expected output `[0.0, 1.0, 4.0, 9.0]` was obtained after CUDA synchronization.

CuPy 14.1.1 does not expose `cp.cuda.Device(0).name` as an attribute. The device name was therefore obtained with the supported runtime property API, `cp.cuda.runtime.getDeviceProperties(0)['name']`; this does not affect the GPU computation result.

## Large-array GPU compute test

The required ROI-sized array was processed on the GPU:

- Shape: `(560, 3840, 3)`
- dtype: `float32`
- Elements: `6,451,200`
- Array bytes: `25,804,800`
- GPU operations: elementwise multiply, `sin`, `cos`, addition, and a `float64` reduction
- CUDA synchronization: performed with `cp.cuda.Stream.null.synchronize()` before results were read back

Observed output:

```text
large_array_sample: [[1.0, 1.0012496709823608], [1.0037474632263184, 1.0049954652786255]]
large_array_checksum: 1967.4738124385476
large_array_finite: True
large_array_gpu_test: PASS
```

**Result: PASS.** The GPU executed the CuPy operations successfully and produced finite, readable results after synchronization.

This was an isolated CuPy compute smoke test only; it was not the P2.19 GPU transform benchmark.

## Pytest regression check

Command:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\.venv\Scripts\python.exe -m pytest
```

Result:

```text
326 passed, 4 warnings in 49.22s
```

Warnings were the existing HLG `RuntimeWarning` messages from `transfer_functions.py` plus a `PytestCacheWarning` caused by access denied while writing `.pytest_cache`.

## Installed/changed packages

Packages installed by the CuPy setup command:

```text
cupy-cuda13x==14.1.1
cuda-pathfinder==1.6.0
cuda-toolkit==13.3.1
nvidia-cublas==13.6.0.2
nvidia-cuda-nvrtc==13.3.33
nvidia-cuda-runtime==13.3.29
nvidia-cufft==12.3.0.29
nvidia-curand==10.4.3.29
nvidia-cusolver==12.2.6.9
nvidia-cusparse==12.8.2.51
nvidia-nvjitlink==13.3.33
```

The complete final `pip list --format=freeze` also contained:

```text
ast_serialize==0.8.0
auto-openmatte==0.1.0
colorama==0.4.6
coverage==7.15.4
iniconfig==2.3.0
librt==0.15.0
mypy==2.3.1
mypy_extensions==1.1.0
numpy==2.5.2
opencv-python-headless==5.0.0.93
packaging==26.3
pathspec==1.1.1
pip==26.2.1
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.3
scipy==1.18.0
typing_extensions==4.16.0
```

All installation was targeted at `.venv`; no global Python package installation was performed.

## Production-code integrity

The following explicitly protected production files had no unstaged or staged diff at final inspection:

- `src/auto_openmatte/processing/transform.py`
- `src/auto_openmatte/processing/luminance.py`
- `src/auto_openmatte/pipeline/compose.py`

No production file was modified by P2.19.1. The repository already contained unrelated working-tree changes and prior untracked artifacts, including `src/auto_openmatte/core/transfer_functions.py`; those were not changed as part of this task.

## Stop condition

CuPy functional and large-array GPU tests passed. Per P2.19.1 instructions, work stops here. The P2.19 GPU transform benchmark remains intentionally unexecuted, and no commit or push was performed.
