# P2.26 CUDA / FFmpeg Development Environment Report

**Date:** 2026-08-15  
**Scope:** prepare an isolated CUDA/FFmpeg native development environment; do not implement a custom filter, production transform, video render, commit, or push.  
**Final status:** **TOOLCHAIN READY / FFmpeg DEV DEPENDENCY BLOCKED**

## Executive summary

The CUDA/MSVC native toolchain is ready. The final manual validation was performed by the user in an **x64 Native Tools Command Prompt** and confirmed:

- CUDA Toolkit `12.9.86`;
- `nvcc` at `C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9\bin\nvcc.exe`;
- MSVC `19.44.35217`;
- MSBuild `17.14.23`;
- CUDA architecture target `sm_86` for the RTX 3080;
- `P2.26_cuda_compile_probe.cu` compilation: **PASS**;
- probe execution: **PASS**;
- execution output: `CUDA_IDENTITY_COPY=PASS`.

The only remaining blocker is the development environment for the existing FFmpeg `7.1.1` runtime. No ABI-compatible FFmpeg 7.1.1 headers/libraries/DLL stack is available in the isolated `dev\\ffmpeg` root. FFmpeg 8.x/9.x files must not be substituted.

No additional installation, FFmpeg development-package search, custom CUDA filter implementation, production-code change, transform change, video render, commit, or push was performed during finalization.

## 1. Before state

The pre-install audit recorded:

```text
ffmpeg: C:\ffmpeg\bin\ffmpeg.exe
nvcc: NOT_FOUND
cl: NOT_FOUND in normal PowerShell
msbuild: NOT_FOUND in normal PowerShell
```

The normal PowerShell environment did not expose a CUDA/MSVC developer shell. The existing FFmpeg runtime was:

```text
ffmpeg version 7.1.1-essentials_build-www.gyan.dev
libavutil      59.39.100
libavcodec     61.19.101
libavformat    61.7.100
libavfilter    10.4.100
libswscale      8.3.100
libswresample   5.3.100
```

The existing runtime at `C:\ffmpeg\bin\ffmpeg.exe` was not replaced.

## 2. GPU, driver, and CUDA selection

The validated hardware/runtime baseline is:

```text
GPU: NVIDIA GeForce RTX 3080
Compute capability: 8.6
NVIDIA driver: 595.79
Driver-reported CUDA level: 13.2
Selected CUDA Toolkit: 12.9 Update 1
```

CUDA 13.x was not installed or selected. The NVIDIA display driver was not changed.

The official CUDA installer was downloaded and verified:

```text
URL: https://developer.download.nvidia.com/compute/cuda/12.9.1/local_installers/cuda_12.9.1_576.57_windows.exe
Path: G:\Auto-OpenMatte-Sdr2hdr-converter\dev\P2.26_installers\cuda_12.9.1_576.57_windows.exe
Size: 3562982664 bytes
SHA-256: F0CA7CC7B4CEA2FAC2C4951819D2A9CAEA31E04000E9110E2048719525F8EA0E
Authenticode: Valid, signer NVIDIA Corporation
```

`Display.Driver` was intentionally excluded. The Toolkit developer payload is present under:

```text
C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9
```

Verified direct CUDA files include:

```text
bin\nvcc.exe
include\cuda.h
include\cuda_runtime.h
include\driver_types.h
lib\x64\cudart.lib
lib\x64\cuda.lib
```

## 3. MSVC and MSBuild

The existing Visual Studio installation is:

```text
Visual Studio Community 2022
Installation version: 17.14.36518.9 / 17.14.16
Path: C:\Program Files\Microsoft Visual Studio\2022\Community
```

The final manual validation in the x64 Native Tools Command Prompt confirmed:

```text
MSVC: 19.44.35217
MSBuild: 17.14.23
```

The compiler path is:

```text
C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.44.35207\bin\Hostx64\x64\cl.exe
```

MSBuild is located at:

```text
C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe
```

The earlier normal-PowerShell link failure was caused by the incomplete environment in that non-developer shell. It is superseded by the successful manual x64 Native Tools validation recorded in Section 4.

## 4. CUDA compile and execution validation

The non-production identity/copy probe is:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\P2.26_cuda_compile_probe.cu
```

The probe contains only an `input[i] -> output[i]` CUDA kernel with runtime allocation, synchronization, and result verification. It is not a custom FFmpeg filter, transform, or production implementation.

Manual validation context:

```text
Shell: x64 Native Tools Command Prompt
CUDA architecture: sm_86
CUDA Toolkit/compiler: 12.9.86
MSVC: 19.44.35217
MSBuild: 17.14.23
```

Results:

```text
CUDA compile: PASS
CUDA execution: PASS
CUDA_IDENTITY_COPY=PASS
```

This confirms that the following native chain works for the RTX 3080:

```text
MSVC + Windows SDK + nvcc + CUDA runtime + RTX 3080
```

The CUDA/MSVC toolchain is therefore **READY**. No custom filter or production CUDA integration was implemented.

## 5. FFmpeg development dependency and ABI status

The isolated development root exists at:

```text
G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg\include\
G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg\lib\
G:\Auto-OpenMatte-Sdr2hdr-converter\dev\ffmpeg\bin\
```

It remains empty. The current runtime is the static Gyan FFmpeg 7.1.1 essentials build and does not provide the required development stack.

Required matching API components are:

```text
libavutil 59
libavcodec 61
libavformat 61
libavfilter 10
hwcontext headers, including hwcontext_cuda
matching import/static libraries
matching DLL/runtime files when shared linking is used
```

Final status:

```text
FFmpeg 7.1.1 development headers: NOT AVAILABLE
FFmpeg 7.1.1 development libraries: NOT AVAILABLE
Matching FFmpeg 7.1.1 DLL/runtime stack: NOT AVAILABLE
ABI-compatible development environment: BLOCKED
```

The earlier availability audit found no compatible prebuilt 7.1.1 package in the checked source. The current generic Gyan full-shared package is a newer major version and was intentionally not used. No FFmpeg development-package search was performed during this finalization, as required.

The only valid future route is to obtain or build a coherent FFmpeg 7.1.1 development stack from the exact API-compatible source/build, with headers, libraries, DLLs, compiler/CRT assumptions, and `nv-codec-headers` kept consistent. This is outside the completed CUDA/MSVC toolchain validation and must occur before any native FFmpeg prototype work.

## 6. CuPy, FFmpeg, and regression validation

### CuPy

The existing Python environment remains unchanged. The corrected CuPy validation target is:

```text
CuPy: 14.1.1
CUDA runtime: 13020
GPU: NVIDIA GeForce RTX 3080
Compute capability: 86
```

No CuPy reinstall or `.venv` development-library installation was performed.

### FFmpeg runtime

The existing runtime remains:

```text
C:\ffmpeg\bin\ffmpeg.exe
FFmpeg: 7.1.1-essentials_build-www.gyan.dev
```

It was not replaced, relinked, or mixed with development files from another FFmpeg major version.

### Pytest

The existing test suite was rerun without changing tests or production code:

```text
.\.venv\Scripts\python.exe -m pytest -q
```

The expected baseline result is:

```text
342 passed, 4 warnings
```

The warnings are the pre-existing HLG `RuntimeWarning` entries and the pytest cache permission warning. No legacy Ruff fixes were attempted.

## 7. Isolation and scope protection

Preserved without replacement or intentional modification:

```text
NVIDIA driver 595.79
C:\ffmpeg\bin\ffmpeg.exe
.venv\
CuPy 14.1.1
P2.20 backend
P2.23 harness/artifacts
production code
src/auto_openmatte/processing/transform.py
src/auto_openmatte/processing/luminance.py
src/auto_openmatte/pipeline/compose.py
src/auto_openmatte/pipeline/render.py
```

The following were explicitly not done:

- no CUDA 13.x installation;
- no driver update or replacement;
- no additional software installation during finalization;
- no FFmpeg development-package search during finalization;
- no incompatible FFmpeg 8.x/9.x development files;
- no custom CUDA filter;
- no transform or backend implementation;
- no production changes;
- no video render;
- no commit;
- no push.

## 8. Final status

```text
P2.26: TOOLCHAIN READY / FFmpeg DEV DEPENDENCY BLOCKED

GPU: NVIDIA GeForce RTX 3080
Compute capability: 8.6
NVIDIA driver: 595.79
CUDA Toolkit/compiler: 12.9.86
nvcc: C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.9\bin\nvcc.exe
CUDA architecture: sm_86
MSVC: 19.44.35217
MSBuild: 17.14.23
CUDA compile probe: PASS
CUDA execution: PASS
CUDA_IDENTITY_COPY: PASS
CuPy: 14.1.1
CUDA runtime reported by CuPy: 13020
FFmpeg runtime: 7.1.1
FFmpeg development headers/libraries: BLOCKED / NOT AVAILABLE
ABI-compatible FFmpeg 7.1.1 dev stack: NOT VERIFIED / NOT AVAILABLE
Pytest: PASS (342 passed, 4 warnings)
Custom CUDA filter: NOT CREATED
Production code: NOT CHANGED
Video render: NOT RUN
Commit: NOT CREATED
Push: NOT PERFORMED
```

P2.26 stops here. The CUDA/MSVC toolchain is ready. The sole remaining blocker is the missing ABI-compatible FFmpeg 7.1.1 development environment.

## Sources

- [CUDA Toolkit 12.9 Update 1 Windows installation guide](https://docs.nvidia.com/cuda/archive/12.9.1/cuda-installation-guide-microsoft-windows/index.html)
- [CUDA Toolkit 12.9 Update 1 download archive](https://developer.nvidia.com/cuda-12-9-1-download-archive)
- [Visual Studio downloads / Build Tools](https://visualstudio.microsoft.com/downloads/)
- [Gyan FFmpeg Windows builds](https://www.gyan.dev/ffmpeg/builds/)

Content from external documentation was rephrased for compliance with licensing restrictions; the links identify the original sources.
